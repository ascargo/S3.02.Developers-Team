<div class="max-w-4xl mx-auto p-4">
    <!-- Header -->
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-semibold">Tasks</h1>
    </div>

    <?php if (!empty($this->error)): ?>
        <div class="mb-4 p-3 rounded bg-red-100 text-red-800">
            <?= htmlspecialchars($this->error) ?>
        </div>
    <?php endif; ?>

    <?php
    // Colors per column
    $cols = [
        'To do'       => 'bg-gray-100',
        'In progress' => 'bg-amber-500/10',
        'Done'        => 'bg-blue-600/10',
    ];
    $badge = [
        'To do'       => 'bg-gray-100 text-gray-700',
        'In progress' => 'bg-amber-500 text-white',
        'Done'        => 'bg-blue-600 text-white',
    ];
    ?>

    <div class="grid md:grid-cols-3 gap-4">
        <?php foreach (['To do', 'In progress', 'Done'] as $col): ?>
            <section class="rounded p-4 <?= $cols[$col] ?>">
                <h2 class="text-xl font-semibold mb-3"><?= $col ?></h2>

                <?php if (empty($this->groups[$col])): ?>
                    <p class="text-sm text-gray-600">No tasks.</p>
                <?php else: ?>
                    <ul class="space-y-3">
                        <?php foreach ($this->groups[$col] as $task): ?>
                            <li class="border rounded p-4 bg-white shadow-sm">
                                <div class="flex items-start justify-between gap-4">
                                    <a href="<?= $this->baseUrl() ?>/task/show/<?= (int)($task->id ?? 0) ?>"
                                        data-modal
                                        class="min-w-0 block">
                                        <h3 class="font-medium truncate">
                                            <?= htmlspecialchars($task->title ?? '') ?>
                                        </h3>
                                    </a>

                                    <div class="flex items-center gap-2 shrink-0">
                                        <a href="<?= $this->baseUrl() ?>/task/show/<?= (int)($task->id ?? 0) ?>"
                                            data-modal
                                            class="px-3 py-1.5 rounded border text-sm">
                                            View
                                        </a>
                                    </div>

                                </div>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php endif; ?>
            </section>
        <?php endforeach; ?>
    </div>
</div>